const ItemListContainer = ({ mensaje }) => {
  return (
    <section className="bg-light text-center p-5 rounded shadow hero">
      <h2>{mensaje}</h2>
    </section>
  );
};

export default ItemListContainer;
